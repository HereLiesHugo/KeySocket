# KeySocket Configuration for Free Render Instance
# Copy this file to .env and adjust values as needed

# Server Configuration
PORT=3000

# Memory Optimization Settings (for 512MB Render free tier)
MAX_CONNECTIONS=999999
NODE_OPTIONS=--max-old-space-size=480 --gc-interval=100 --expose-gc

# Connection Limits (unlimited)
MAX_SSH_SESSIONS_PER_IP=999999
CONNECTION_TIMEOUT=180000
CLEANUP_INTERVAL=30000
MAX_OUTPUT_BUFFER_SIZE=8192

# Security (optional)
# ALLOWED_HOSTS=example.com,192.168.1.0/24